name: marketplace_user
description: "MarketPlace User App - ตลาดนัดออนไลน์"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  shared_preferences: ^2.2.0
  provider: ^6.1.0
  intl: ^0.19.0
  cached_network_image: ^3.3.0
  google_fonts: ^6.1.0
  image_picker: ^1.0.7
  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/splash_logo.png
    - assets/home_logo.png
